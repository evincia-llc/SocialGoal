# Test-project-only analyzer adjustments (inherits the SDK defaults otherwise).

[*.cs]
# Underscored test names are the suite convention (matches the legacy
# characterization suite; NUnit idiom).
dotnet_diagnostic.CA1707.severity = none
# Inline expected-value arrays in assertions are clearer than hoisted fields.
dotnet_diagnostic.CA1861.severity = none
